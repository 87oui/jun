<header>
  <h1 role="banner">
    <a href="{{ site.url }}" rel="home">{{ site.name }}</a>
  </h1>
  <nav role="navigation">
    {% include 'partials/menu.twig' with {
      items: menu.get_items
    } %}
  </nav>
</header>
<h1>
  {{ title }}
</h1>
