<?php
if ( ! function_exists( 'jun_get_sidebar_config' ) ) {
	/**
	 * サイドバーに表示させる値を設定する
	 *
	 * @return array サイドバーに表示させる値
	 */
	function jun_get_sidebar_config() {
		$sidebar_context = array();

		return $sidebar_context;
	}
}
